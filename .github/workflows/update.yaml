---
name: update

on: push

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - name: Check out source code
        uses: actions/checkout@v2

      - name: Update
        uses: docker://garethjevans/uc:main
        with:
          entrypoint: uc
          args: --path plugins.txt --display-updates-only
